resources:
  - name: myCronTrigger
    type: CronTrigger
    configuration: 
      interval: "*/2 * * * *"

pipelines: 
  - name: myPipeline
    steps: 
      - name: myScheduledStep
        type: Bash
        configuration: 
          inputResources: 
            - name: myCronTrigger 
        execution:
          onExecute:
            - printenv
            - docker images
            - test "$ENV1" == "value1"
            - test "$ENV2" == "value2"
